---
description: After finishing work, ask user if they want to git commit and push
---

After completing any task or set of changes, always ask the user:

"ต้องการ commit & push ขึ้น GitHub ไหมครับ?"

If user says yes:
// turbo
1. Run `git add .` in `c:\xampp\htdocs\test`
// turbo
2. Run `git commit -m "<descriptive message about what changed>"` in `c:\xampp\htdocs\test`
// turbo
3. Run `git push` in `c:\xampp\htdocs\test`
